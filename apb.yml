version: '1.0.1'
name: ssh_tunnel
description: Role for fast creating SSH tunnels
bindable: False
async: optional
metadata:
  displayName: ssh_tunnel
plans:
  - name: default
    description: This default plan deploys ssh_tunnel
    free: True
    metadata: {}
    parameters: []